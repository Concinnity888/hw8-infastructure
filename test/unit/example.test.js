describe('Simple Test Case', () => {
  it('Should return 4', () => {
      expect(2 + 2).toBe(4);
  });

  it('Should return empty string', () => {
    expect('').toBe('');
  });

  it('Should return boolean', () => {
    expect(true).toBe(true);
  });

  it('Should return boolean', () => {
    expect(true).toBe(true);
  });

  it('Should return boolean', () => {
    expect(true).toBe(true);
  });
});
